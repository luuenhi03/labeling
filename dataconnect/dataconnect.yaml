specVersion: "v1beta"
serviceId: "labelme"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "labelme-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
